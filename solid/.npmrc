# Supply-chain hardening (suite default — see prod-solid-server ADR-0011):
# disable npm install/lifecycle scripts so no fetched package can run a
# postinstall/preinstall/prepare payload. This is a private integration module,
# not a published package; it nonetheless follows the suite supply-chain rule.
ignore-scripts=true
